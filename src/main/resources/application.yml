spring:
  application:
    name: listener-app
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8080

# ──────────────────────────────────────────────────
# OpenAI Whisper — replace the placeholder below
# or set the OPENAI_API_KEY environment variable.
# ──────────────────────────────────────────────────
whisper:
  api-key: YOUR_OPENAI_API_KEY_HERE
  url: https://api.openai.com/v1/audio/transcriptions
  model: whisper-1

# ──────────────────────────────────────────────────
# GitHub — replace each placeholder below
# or set the corresponding environment variables.
# ──────────────────────────────────────────────────
github:
  token: YOUR_GITHUB_TOKEN_HERE
  owner: YOUR_GITHUB_USERNAME_HERE
  repo: YOUR_GITHUB_REPO_NAME_HERE
  branch: main
  inbox-path: inbox

# ──────────────────────────────────────────────────
# Audio compression (requires FFmpeg)
# ──────────────────────────────────────────────────
audio:
  whisper-max-bytes: 25000000    # 25 MB — Whisper hard limit
  min-bitrate-kbps: 32           # quality floor — below this, audio is too degraded
  max-bitrate-kbps: 128          # cap — higher is unnecessary for voice
  ffmpeg-path: ffmpeg            # system PATH or absolute path to binary

# ──────────────────────────────────────────────────
# Retry — applied to Whisper + GitHub API clients
# ──────────────────────────────────────────────────
retry:
  max-attempts: 3
  initial-delay-ms: 1000
  multiplier: 2.0

# ──────────────────────────────────────────────────
# Dead Letter Queue
#   whisper-unreachable/ — audio stored for later transcription
#   github-unreachable/  — transcript stored for later commit
# ──────────────────────────────────────────────────
dlq:
  base-path: ./dlq
  retry-interval-ms: 300000      # 5 minutes
  max-retry-attempts: 5
